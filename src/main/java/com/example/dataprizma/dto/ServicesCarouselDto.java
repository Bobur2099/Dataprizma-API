package com.example.dataprizma.dto;


import com.example.dataprizma.model.ServicesCarousel;
import com.example.dataprizma.service.ServicesService;
import lombok.Getter;
import lombok.NoArgsConstructor;
import lombok.Setter;
import org.yaml.snakeyaml.util.EnumUtils;

import javax.persistence.Column;

@NoArgsConstructor
@Getter
@Setter
public class ServicesCarouselDto {

//    private Long Id;
//    private Long servicesId;
//    private Long carouselId;
//    private String serviceTopic;
//    private String serviceHeader;
//    private String header;
//    private String text;
//
//    public ServicesCarouselDto(ServicesCarousel servicesCarousel) {
//        if (servicesCarousel.getId() != null)
//            setId(servicesCarousel.getId());
//        if (servicesCarousel.getServices() != null) {
//            setServicesId(servicesCarousel.getServices().getId());
//            setServiceHeader(servicesCarousel.getServices().getHeader());
//            setServiceTopic(servicesCarousel.getServices().getTopic());
//        }
//        if (servicesCarousel.getSerCarousel() != null) {
//            setCarouselId(servicesCarousel.getSerCarousel().getId());
//            setHeader(servicesCarousel.getSerCarousel().getHeader());
//            setText(servicesCarousel.getSerCarousel().getText());
//        }
//    }


}
